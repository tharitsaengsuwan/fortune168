<nav class="navbar sticky-top navbar-expand-lg border-bottom">
    <div class="container-fluid">
        <a class="navbar-brand logo" href="/">Fortune168</a>
        <button class="navbar-toggler custom-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/fortuneTellers">find a fortune teller</a>
            </div>
            <div class="navbar-nav ms-auto">
                <% if(!currentUser) { %>
                    <a href="/login" class="nav-link">Login</a>
                    <a href="/register" class="nav-link">Register</a>
                    <% } else { %>
                        <a href="/users/schedules/<%= currentUser._id.toString()%>" class="nav-link">My Schedules</a>
                        <a href="/users/<%= currentUser._id.toString()%>" class="nav-link">Profile</a>
                        <a href="/logout" class="nav-link">Logout</a>
                        <% } %>
            </div>
        </div>
    </div>
</nav>